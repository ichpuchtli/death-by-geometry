# CLAUDE.md — Death by Geometry

> Context document for AI assistants working on this codebase.
> Read this file, `PRD.md`, `ENEMY_DESIGNS.md`, and `TASKS.md` before making changes.

**Workflow rules:**
1. After each change, commit and push to master.
2. **MANDATORY: Update this CLAUDE.md file** after every code change to reflect the current state of the codebase. This includes: new/changed config values, new settings, architectural changes, new files, completed work items, and any other information that would help a future AI assistant understand the codebase. This update must be part of the same commit as the code change. **Also update the relevant skill file** if the change touches audio, enemies, or combat feedback systems.
3. **MANDATORY: Test every change with a Playwright test** before committing. Use the `/playwright` skill to write and run tests that verify your changes work correctly.

---

## What This Is

Death by Geometry is a browser-based twin-stick arcade shooter inspired by Geometry Wars and [Grid Wars](https://worldofstuart.excellentcontent.com/grid/wars.htm). Originally a Python 2/Pygame desktop game by Sam Macpherson (2013), rebuilt as TypeScript + raw WebGL deployed to GitHub Pages.

**Play it:** https://ichpuchtli.github.io/death-by-geometry/

Entirely client-side. WebGL renders everything (bloom, grid distortion, particle trails). Audio via Web Audio API with procedural synthwave music. Mobile uses twin-stick virtual joysticks.

---

## Project Vision

**Neon chaos** — dozens to hundreds of geometric enemies swarming the player with Geometry Wars-level visual spectacle. Difficulty curve:

- **0-30s (Tutorial):** Gentle. Rhombus, pinwheel, rare blackhole.
- **30-120s (Ramp Up):** Swarms + walls. Square and blackhole added. 20-40 enemies.
- **120-240s (Mid Game):** Formations. Sierpinski, blackhole. 30-60 enemies.
- **240-400s (Intense):** Ambush + cascade. Mandelbrot miniboss. 40-80 enemies.
- **400s+ (Chaos):** Maximum spawn rates. Screen constantly full.

Cadence system alternates **burst windows** (2x spawn rates) and **breathers** (trickle only) for tension/release. Player has 5 lives. Weapon auto-upgrades at score milestones — shotgun-style: slow, deliberate cadence (`WEAPON_STAGES` shotDelay ~280–320ms) that ramps the number of parallel pellets (2→6) and cone width rather than fire speed, so shooting rewards aim over spraying a stream.

---

## Tech Stack

| Component | Technology |
|---|---|
| Language | TypeScript (strict mode) |
| Build | Vite + vite-plugin-glsl |
| Rendering | Raw WebGL 2 (no framework) |
| Audio | Web Audio API (WAV SFX + procedural music) |
| Deploy | GitHub Actions → GitHub Pages |
| Package Manager | npm |

---

## Directory Structure

```
web/src/
├── index.ts                    # Entry point
├── game.ts                     # Main game loop, state machine, orchestrator
├── design-lab.ts               # BlackHole visual sandbox (press D from menu)
├── threat-lab.ts               # Threat Lab — playable BlackHole threat-preset A/B arena (?threat=1)
├── gallery.ts                  # Specimen Gallery — visual catalog of every entity/component (?gallery=1)
├── particle-lab.ts             # Particle Lab — dust field + swirl/thruster/sparklet sandbox (?particles=1)
├── circle-lab.ts               # Circle Lab — Circle-enemy tracking-model + visual-DNA sandbox (?circles=1)
├── taxonomy-lab.ts             # Taxonomy Lab — labeled anatomy chart of every BlackHole visual effect (?taxonomy=1)
├── config.ts                   # ALL compile-time constants
├── settings.ts                 # Runtime-tunable settings (localStorage)
├── glsl.d.ts                   # GLSL import declarations
│
├── ai/                         # AI agent (see "AI Agent" section)
│   ├── policy.ts               # Tiny MLP + pure-TS forward pass (shared Node/browser)
│   ├── observation.ts          # Egocentric radar observation encoder
│   ├── action.ts               # Policy output → twin-stick action
│   ├── bot.ts                  # Browser-side agent (loads trained-policy.json)
│   └── trained-policy.json     # Evolved weights (generated by scripts/train.ts)
│
├── sim/                        # Headless "digital twin" for training
│   ├── headless-game.ts        # Gameplay update loop, renderer/audio stubbed
│   ├── scripted-input.ts       # InputSource driven by the agent's action
│   └── stubs.ts                # No-op renderer/audio/camera/HUD stand-ins
│
├── core/
│   ├── vector.ts               # Vec2 (immutable-style math)
│   ├── camera.ts               # Camera follow + screen shake
│   ├── input.ts                # Keyboard/mouse/touch input + `InputSource` iface + AI bot override
│   ├── collision.ts            # Circle-circle collision detection
│   ├── audio.ts                # AudioManager (SFX + ProceduralMusic)
│   ├── haptics.ts              # Vibration API wrapper
│   └── run-stats.ts            # RunStats + medal computation
│
├── renderer/
│   ├── sprite-batch.ts         # Batched WebGL line/triangle renderer
│   ├── bloom.ts                # Multi-pass bloom post-processing
│   ├── grid.ts                 # Reactive spring-mass grid with gravity wells
│   ├── trails.ts               # Per-entity trail ring buffers
│   ├── starfield.ts            # Background star dots
│   ├── particle-field.ts       # Ambient "cosmic dust" — orbiting/swirling motes + injected bursts + heat-bias (?particles lab)
│   ├── debris-field.ts         # Geometry shatter — dying units break into tumbling per-edge line fragments (?particles lab)
│   └── webgl-context.ts        # Shader compilation helpers
│
├── entities/
│   ├── entity.ts               # Base Entity class
│   ├── player.ts               # Player ship (movement, shooting, weapon progression)
│   ├── wingman.ts              # AI co-op ally (trained policy drives a second ship beside the player)
│   ├── bullet.ts               # BulletPool (object pooling) with `trailId` for lifecycle
│   ├── explosion.ts            # ExplosionPool (line particles)
│   ├── crosshair.ts            # Aim indicator (chevrons)
│   └── enemies/                # See enemy-reference skill for full roster
│       ├── enemy.ts            # Base Enemy class
│       ├── rhombus.ts          # Tier 1 — tracker
│       ├── pinwheel.ts         # Tier 1 — bouncer
│       ├── blackhole.ts        # Tier 3 — gravity well, absorbs enemies (supernova on overload)
│       ├── sierpinski.ts       # Tier 3 — fractal breakup
│       ├── mandelbrot.ts       # Miniboss — 3 HP stages
│       ├── [children]          # circle, shard, square2, minimandel
│       └── [9 unwired]         # triangle, fibspiral, mobius, koch, etc.
│
├── systems/
│   ├── lifecycle-system.ts     # Centralized enemy + bullet trail lifecycle
│   ├── combat-system.ts        # Kill processing, heat, hitstop, kill signatures
│   ├── spawn-system.ts         # WaveManager execution, caps, spawn SFX, formation telegraphs
│   ├── gravity-system.ts       # BlackHole attraction/absorption/supernova, player pull, grid wells, circle flocks
│   ├── boss-system.ts          # Generic boss-encounter state machine (Sierpinski + Mandelbrot)
│   └── separation.ts           # Shared pairwise enemy separation (used by Game + headless sim)
│
├── spawner/
│   ├── enemy-factory.ts        # Shared `createEnemy()` factory
│   ├── spawn-patterns.ts       # Enemy pools + formation generators
│   └── wave-manager.ts         # Event-based spawn scheduler + cadence
│
└── ui/
    ├── hud.ts                  # Score, lives, game over summary card
    ├── settings-panel.ts       # Settings panel (desktop sidebar + mobile)
    ├── virtual-joystick.ts     # Mobile twin-stick joysticks
    └── offscreen-indicators.ts # Edge arrows for off-screen enemies
```

---

## Architecture Overview

> **Refactor status:** `game.ts` is now a ~1,240-line orchestrator. The original god-object responsibilities are extracted into `systems/` (`LifecycleSystem`, `CombatSystem`, `SpawnSystem`, `GravitySystem`, `BossSystem`). All checklist items in `docs/REFACTOR_PLAN.md` are complete.

### Game Loop (`game.ts`)

`update(dt)`: player movement → **GravitySystem.applyPlayerPull()** → bullets → **GravitySystem.applyAttraction() + updateFlocks()** → enemy AI → **enemy separation** → **SpawnSystem.update()** (wave manager execution) → collision → **CombatSystem.processKills()** → staggered child spawns/heat decay → LifecycleSystem trail update/cleanup → explosions/**GravitySystem.updateGravityWells()**/grid/camera → music intensity.

**Enemy separation** (`separateEnemies()`): Per-frame O(n²/2) pairwise position correction. If two enemies overlap (distance < sum of collision radii + `ENEMY_SEPARATION_BUFFER`), push both apart proportionally. Near-zero distance uses deterministic direction (index-derived, not random) for consistent frame-to-frame separation. Heavy overlaps (>50% of minDist) get 1.5× push strength for faster cluster resolution. BlackHoles immovable (weight 0), minibosses resist (0.25), all others 50/50. Enemies inside a BlackHole gravity well exempt (gravity wins). Spawning enemies participate in separation during the final 70% of their spawn animation (allows clustered formations to spread before spawn completes).

`render()`: grid → starfield → entities (normal blend) → trails + explosions (additive) → bloom → HUD.

### Systems

`Game` is being refactored into focused systems owned by `Game`:

- **`LifecycleSystem`** (`web/src/systems/lifecycle-system.ts`) — owns the `TrailSystem` and automates trail attach/detach for enemies and bullets. `Game` calls `spawnEnemy()` / `spawnBullet()` when entities are created, `cleanupEnemies()` / `updateBulletTrails()` each frame, and `clear()` / `clearBulletTrails()` on reset. `cleanupEnemies()` filters the enemies array **in place** (returns the same reference) so systems holding the array stay in sync — `Game` never reassigns `this.enemies` (uses `.length = 0` to reset), keeping the single shared reference valid across `Game`, `CombatSystem`, and `SpawnSystem`.
- **`CombatSystem`** (`web/src/systems/combat-system.ts`) — owns kill processing, heat value, hitstop accumulation, kill-signature VFX, and staggered child-spawn timing. `Game` calls `processKills(result)` after collision detection, `update(dt, gameTime)` each frame (handles heat decay/survival pressure and ready staggered spawns), and `render(renderer)` during the additive blend pass. `Game` reads `combat.heatValue` to drive bloom, arena border color, and music intensity.
- **`SpawnSystem`** (`web/src/systems/spawn-system.ts`) — executes `WaveManager` spawn requests: cap enforcement (max enemies, ≤4 BlackHoles, elite cap), ambush spawn duration, edge-push for player-proximity spawns, trail registration, spawn grid ripples, spawn SFX (with formation leakthrough suppression), and the formation telegraph lifecycle. `Game` calls `spawn.update(dt)` each frame, `spawn.updateTelegraphs(dt)` from combat-feedback timers, `spawn.renderTelegraphs(renderer)` during the normal blend pass, and `spawn.clear()` on reset. `WaveManager` remains the scheduler; `SpawnSystem` executes its requests.
- **`GravitySystem`** (`web/src/systems/gravity-system.ts`) — owns all BlackHole physics: enemy attraction + absorption + overload supernova (`applyAttraction(dt)`), bullet gravity bending, player pull (`applyPlayerPull(dt)`), grid gravity-well registration (`updateGravityWells()`), the elastic circle-flock centroids ejected by supernovae (`updateFlocks()`), and the gravity-well exemption set used by separation (`getEnemiesInGravityWell()`). Gravity has an **inescapable core**: pull ×`BH_CORE_PULL_MULT` (2.5) inside `BH_CORE_RADIUS_FRACTION` (0.8) of the attract radius — with defaults (radius 500, pull 24) tracking rhombuses are captured within ~400px. Supernova detonation (`detonate()`) is **checked every frame** (a latent bug previously gated it behind the absorb branch, unreachable at full mass); payload = 3× circles + 8 shards + 1000 particles + 3 shockwave rings (`update(dt)` advances them, `renderEffects(renderer)` draws them in the additive pass) + 'subdrop' detonation sound. `update(dt)` also feeds `audio.setBlackHoleStress()` — the wobbling sub-bass instability loop. Owns `circleFlocks`, `shockRings`, and the per-BlackHole supernova-warning set; `Game` calls `gravity.clear()` on reset/respawn. Game-owned feedback (border pulse, hitstop, supernova screen flash, haptics) is routed back via the `onSupernovaWarning` / `onSupernovaDetonate` callbacks.
- **`BossSystem`** (`web/src/systems/boss-system.ts`) — a generic `BossEncounter` state machine (idle → time-trigger → warning → spawn → active → defeated, with shockwave-kill → respawn) instantiated twice from per-boss config: Sierpinski (no active phase) and Mandelbrot (active-phase hook spawns MiniMandels + handles stage transitions). Owns all boss timers/refs and the shared saved spawn-rate multiplier. `Game` calls `boss.update(dt)` each frame, `boss.renderHud(hud)` in the HUD pass, and `boss.reset()` on game start. `CombatSystem` boss-defeat callbacks call `boss.onSierpinskiDefeated()` / `boss.onMandelbrotDefeated()`. Game-owned feedback (warning border pulse, stage-break hitstop) is routed via the `onWarning` / `requestHitstop` callbacks.
- **`enemy-factory.ts`** (`web/src/spawner/enemy-factory.ts`) — shared `createEnemy(type, pos, isElite, tier?)` used by `Game`, `CombatSystem`, and `SpawnSystem`.
- **`run-stats.ts`** (`web/src/core/run-stats.ts`) — `RunStats` interface and `computeMedals()` extracted to avoid a circular dependency between `game.ts` and `CombatSystem`.

### Rendering Pipeline

```
Scene FBO → Bloom (brightness extract → Gaussian blur → composite + chromatic aberration) → Screen
```

Bloom: ping-pong FBOs, half-res on mobile. Grid: own shader with gravity well uniforms (spacetime fabric). Trails: ring buffers with additive blending.

### Config System

**Every tunable value** lives in `config.ts` (compile-time) or `settings.ts` (runtime, localStorage). Nothing hardcoded in entity classes.

**`config.ts`** is a barrel that re-exports domain files under `web/src/config/` (`world`, `player`, `bullet`, `enemy`, `spawner`, `effects`, `ui`, `combat`, `boss`, `audio`, `medals`). Import from `'./config'` as before; add a new constant to the matching domain file.

**`settings.ts`** (runtime-tunable via settings panel):
- Gameplay: spawn rate, lives, player/enemy speed, fire rate, starting phase, max enemies
- Visual: bloom intensity, trail length, resolution scale (0.25–2.0x)
- BlackHole gravity: attract radius, enemy/player pull, grid mass, perspective depth
- Grid physics: anchor stiffness, damping, max displacement
- GPU Stress: arena size (800–6400 × 500–4000), grid spacing/substeps/stiffness, bloom passes/threshold/radius
- Camera: zoom scale (0.5–1.5)
- Toggles: vulnerable during spawn; **AI Wingman** (spawn an AI-controlled ally that fights beside you)

### AI Agent (self-playing bot)

A neural-network policy trained by **neuroevolution** plays the game for automated/soak
testing and live demos. Full details in **`docs/AI_AGENT.md`**.

- **Watch it:** press **B** in-game, or load with **`?bot=1`** (auto-starts + auto-restarts on death). A "🤖 AI AGENT PLAYING" badge shows while active.
- **Digital twin** (`web/src/sim/headless-game.ts`): the real gameplay loop with renderer/audio/camera **stubbed** (`sim/stubs.ts`). Reuses the actual Player, enemy classes, bullet pool, collision, WaveManager, and all five systems, so dynamics match the live game. Runs headless in Node for fast training.
  - This is enabled by two refactors: renderer/GL classes (`Renderer`, `SpringMassGrid`, `Camera`, `AudioManager`, `ExplosionPool`, `HUD`) are imported with **`import type`** everywhere they're used only as types, so the headless import graph never loads a WebGL/GLSL module; and `Player` now depends on an **`InputSource`** interface (implemented by DOM `Input` and headless `ScriptedInput`).
- **Policy** (`web/src/ai/policy.ts`): a small MLP (`[obs,24,16,4]`) with a pure-TS forward pass — no ML framework. Shared verbatim by trainer and browser; weights serialize to `trained-policy.json` (imported into the bundle).
- **Observation** (`ai/observation.ts`): egocentric, order-invariant 16-sector "radar" (nearest-enemy proximity + closing speed per sector) + player pos/vel + wall clearances + nearest-enemy vector. **Action** (`ai/action.ts`): move vector + aim vector (fire always on).
- **Train:** `cd web && npx tsx scripts/train.ts [--gens=30 --pop=40 --elite=8 --secs=25 --dt=33 --eps=2 --survW=10 --scoreW=0.004 --phases=tutorial,rampUp,midGame,intense,chaos]`. Cross-Entropy Method: sample a population of weight vectors, score each by survival-time × `survW` + score × `scoreW` in the twin, refit the search distribution to the top elites. Writes/checkpoints `src/ai/trained-policy.json` each generation. `npx tsx scripts/eval.ts` prints survival/score vs a do-nothing baseline.
- **Live training dashboard:** add `--live [--port=8787]` and `train.ts` starts a zero-dependency Node `http` server (serves `scripts/train-dashboard.html`) with real-time fitness chart, per-phase survival/score of the current best, generation/candidate progress, and evolving per-layer weight heatmaps. Polls `/progress` JSON ~600ms; CEM loop yields via `setImmediate` per candidate so the server stays responsive. See `docs/AI_AGENT.md`.

### Game States

`'menu'` | `'playing'` | `'gameover'` | `'design_lab'`

Controls: WASD move, mouse aim, click/hold shoot, F auto-fire, M mute, P pause (shows config), D design lab, Space restart, **B toggle AI agent** (also `?bot=1` URL auto-starts it).

**Threat Lab** (`?threat=1`): a playable BlackHole tuning arena (`web/src/threat-lab.ts`) that boots instead of the game. One BlackHole vs. the real player ship + a constant rhombus trickle that tracks the player. Keys **1-4** switch full threat presets (`THREAT_PRESETS`: PRODUCTION / DREADNOUGHT / CATACLYSM / SINGULARITY — preset 1 mirrors shipped config; keep it in sync) varying HP, feed threshold, attract radius, enemy/player pull (+ "inescapable core" multiplier inside `coreRadiusFrac` of the radius), warning window, detonation payload (circles + shards), particle/shockwave/flash intensity, and supernova sound variant. **E** feeds the hole to critical, **Q** detonates instantly, **A** cycles the detonation sound independently of the preset (mix-and-match A/B), **R** resets. Faint rings visualize the attract radius + core. Supporting engine changes: `BlackHole.destabilizeDuration` instance field (config `SUPERNOVA_DESTABILIZE_MS`), `AudioManager.playSupernovaWarning(durationMs)`, and `AudioManager.playSupernovaVariant('classic'|'subdrop'|'doom'|'quake', absorbed)` — three deep-bass detonation sounds. `window.threatLab` exposed. Flow test: `tests/flows/77-threat-lab.yml`.

**Specimen Gallery** (`?gallery=1`): a standalone visual catalog (`web/src/gallery.ts`) that boots instead of the game and renders every ship, enemy (all states/variants/tiers), BlackHole visual mode, looping spawn animation, projectile, effect, and UI component in a single labeled grid — built for screenshot-based visual review (of both humans and AI models). Auto-fits the whole roster into one frame; `window.gallery` is exposed. Toggles: Space pause, G reactive grid, B bloom, S starfield, L labels. Flow test: `tests/flows/76-gallery.yml`.

**Particle Lab** (`?particles=1`): a playable sandbox (`web/src/particle-lab.ts`) for the "cosmic dust" aesthetic (inspired by a gravity-sandbox demo) plus three companion particle effects, all toggle/tunable live so their look can be dialed in before porting into the game. Fly the real ship around one BlackHole + a rhombus trickle. The star is the reusable **`ParticleField`** (`web/src/renderer/particle-field.ts`): a few hundred additive "dust" motes in world space that are pulled **and swirled** (tangential force) around attractors into glowing accretion disks, drawn as velocity-stretched streaks whose brightness/hue rise with speed, wrapped within the camera view so density stays even. It also injects short-lived **bursts** (`spawnBurst()`) reused by the thruster wake + impact sparklets. Enemies spawn **on the well's rim** (a wave on reset, a fast trickle, and **Space** for an on-demand ring) so there's always something interacting with the hole; the ship is **tethered to a screen-sized box around the BlackHole** (`tetherPlayerToHole()`) so the hole and its spawns never scroll off-view (without it, flying away made it look like nothing was spawning); the lab's enemy gravity mirrors production including the **inescapable core** (`BH_CORE_RADIUS_FRACTION`/`BH_CORE_PULL_MULT`) so trackers get captured instead of escaping toward the player. Effects (number/letter keys toggle): **1** ambient dust field · **2** swirl-on-gravity (rhombuses spiral in instead of falling straight — a tangential term added to enemy attraction; subtle default `gravitySwirl` 0.35, **Z/X** 0–6 in 0.1 steps, and lab enemies use long 46-step trails so even a gentle path curve reads) · **3** thruster wake (dust kicked out behind the moving ship) · **4** impact sparklets (a momentum-carrying spark puff on each bullet hit) · **C** geometry shatter (units destroyed by bullet/contact break along their OWN wireframe edges into tumbling rigid fragments — see `DebrisField` — instead of the generic radial particle burst; absorbed units still just warp+vanish) · **V** death warp (tidal spaghettification: a unit's geometry stretches + twists toward the hole in its final moments inside `warpReach` px, via `Renderer.setWarp`). Life-cycle keys: **F** feed-to-critical (fill the hole to `MAX_ABSORB` so destabilize→supernova can be watched on demand), **T** detonate (force overload now) — on overload the dust field erupts outward in a hot ring and the hole sheds its mass + recycles, looping the cycle. Tuning keys: **Q/W** dust density, **A/S** dust orbit swirl, **Z/X** gravity swirl, **E/D** streak length, **G** grid, **B** bloom (zeroes `bloom.intensity` and still composites), **R** reset.

**Taxonomy Lab** (`?taxonomy=1`): a labeled anatomy chart (`web/src/taxonomy-lab.ts`, Gallery-style zoom-to-fit grid) of **every visual effect layered onto a BlackHole**, built to answer "we have particles, but there's ALSO something else swirling around the hole and I don't know what it's called." The answer it makes visible: a hole has **three distinct in-class particle systems** (all separate from the external ambient `ParticleField` dust) — **Swirl Arms** (`BlackHole.swirlParticles` → `renderSwirlArms`, 4 spiral accretion streams — *this is "the swirling thing"*), **Orbit Dots** (`horizonParticles`, dots circling the ring with trail arcs), and **Infall Streaks** (`infallStreaks`, matter raining inward). Each is rendered **solo** in its own labeled cell via new component-visibility flags on `BlackHole` — `showSwirlArms` / `showOrbitDots` / `showInfallStreaks` (all default `true`, so the shipped hole is unchanged; the lab flips them to isolate one system at a time). Cells: the 4 accretion-disc **modes** (dense/haze/corona/molten), the **anatomy** row (Static reference → Swirl Arms → Orbit Dots → Infall Streaks), and **related systems** (Ambient Dust Field = the `ParticleField` already ported to circles, Destabilize Telegraph, Hit Sparks, Full-combined). Each cell carries a name + a one-line descriptor naming the source field, so you can pick which layer to graft onto the Circle enemy. Toggles: Space pause · G grid · B bloom · L labels. `window.taxonomyLab` exposed. Flow test: `tests/flows/85-taxonomy-lab.yml`.

**Dust ↔ black-hole life-stage coupling** (answers "is the particle effect woven into the stages of life of the hole"): `buildAttractors()` sets each hole's dust `strength` to scale with swallowed mass and a `heat` value (0..1) that tracks instability and spikes to near-white during the destabilize warning — `ParticleField` biases nearby motes toward an amber-hot hue by `heat`, and `field.brightness` rises while destabilizing. So the dust visibly heats + tightens as the hole fills, glows white at critical, and blows out on supernova.

**Circle Lab** (`?circles=1`): a playable sandbox (`web/src/circle-lab.ts`) for tuning the **tracking behaviour and visual DNA** of the BlackHole-ejecta Circle enemy — built to answer "the tracking feels gimmicky, needs elasticity/unpredictability, and the circles should carry BlackHole DNA (particles/swarm)". Fly the real ship; a flock of `LabCircle`s (momentum-carrying, each with per-instance `jitter`/`wobblePhase`/`spinDir` personality) hunts you with one of **five selectable tracking models** (number keys): **1** direct lead (the shipped behaviour, hard-set intercept velocity — the A/B baseline) · **2** **orbital** (default — the player is a *gravity well*: a central spring pull that strengthens with distance + retained momentum + light drag, so circles overshoot, curve into an orbit, then fall back into your gravity — the elastic feel the hard-lead lacks; a faint capture ring visualises the reach) · **3** elastic spring (underdamped spring to a *lead* point → springy overshoot + oscillation) · **4** serpentine (lead heading + per-circle sine wander → unpredictable weaving approach) · **5** swarm/boids (seek + separation + cohesion + alignment → an emergent flowing blob). **Visual DNA layers** (letter keys, all default on): **O** orbiting satellite motes (accretion) · **P** shed dust (each circle leaks motes into a shared `ParticleField` so it reads as a shard of the hole that spat it out) · **U** pulse halo (per-circle breathing glow) · **T** velocity streak (comet tail). Body = dark event-horizon core + bright cyan accretion ring. Tuning keys: **Z/X** primary param (springK for orbital/spring, wobble for serpentine, speed for lead) · **C/V** drag · **N/M** speed cap · **[ ]** flock count · **SPACE** eject a fresh ring · **G** grid · **B** bloom · **R** reset. Shooting pops circles (`playKillSignature('circle')` + dust spray); player contact is non-lethal here (flash + elastic bounce + light shake) so you can keep observing. `window.circleLab` exposed. Flow test: `tests/flows/84-circle-lab.yml`. **Not yet ported to the live game** — preview/tuning stage; port the winning model into `CircleEnemy.update` next.

Config defaults in `config/effects.ts` (`PARTICLE_FIELD_*`; `SHATTER_EDGE_SUBDIV/EJECT_SPEED/IMPACT_SHARE/SPIN/DRAG/LIFE/POOL_SIZE`; `DEATH_WARP_STRETCH/TWIST`). Shared, reusable pieces for the later game port: `renderer/debris-field.ts` (`DebrisField.shatter(points, cx, cy, color, impactAngle, impactSpeed)`), `Renderer.setWarp(cx,cy,k,stretch,twist,radius)`/`clearWarp()` (per-vertex tidal transform applied to whatever is drawn while active), and `FieldAttractor.heat`. `window.particleLab` exposed. Flow test: `tests/flows/80-particle-lab.yml`. **Not yet wired into the live game** — this is the preview/tuning stage; port the picks next.

---

## Build & Run

```bash
cd web
npm install
npm run dev      # Dev server with hot reload
npm run build    # Production build
```

TypeScript check: `npx tsc --noEmit` from `web/`

---

## Detailed Reference (Progressive Disclosure)

These skill files are loaded contextually when relevant:

| Skill | Contents | When loaded |
|---|---|---|
| **`.claude/skills/audio-system/`** | SFX, procedural music, ElevenLabs pipeline, kill signatures | Audio work |
| **`.claude/skills/enemy-reference/`** | Enemy roster, enable/disable, kill families, BlackHole, miniboss, elites, design lab | Enemy/spawner work |
| **`.claude/skills/combat-feedback/`** | Hitstop, kill signatures, heat system, recovery window, phase transitions, telegraphs, medals, game over card | Combat/HUD work |

Full development history: **`docs/DEVELOPMENT_HISTORY.md`**

### Completion Status
- Phases 1-3 (MVP, Visual Polish, Mobile & Audio): **Complete**
- New Enemies Expansion + Post-expansion Polish: **Complete**
- ROADMAP Phases 1-4 (Combat Feedback, Elites, Heat/Recovery, Miniboss): **Complete**
- ROADMAP Phase 6 (End-of-Run Story + Medals): **Complete**
- BlackHole Design Lab: **Complete** (4 new visual variants: Radiant Collapse, Swirl, Unstable Mass, Event Horizon)
- Circle gravity immunity: **Complete** (Circles immune to BlackHole pull + absorption)
- Enemy separation steering: **Complete** (Grid Wars-style pairwise push, replaces spawn-only separation)
- Formation group spawn sound: **Complete** (6 procedural "gatling brrrr" variants per formation type, individual SFX suppressed for 6+ enemy formations, first 2 leak through at 15% volume)
- Clustered formation spawns: **Complete** (all formation types spawn enemies at a single point; separation steering organically spreads them into blobs/lines/rings)
- Sierpinski fractal breakup: **Complete** (3-tier cascade: 1 boss → 3 medium → 9 small. Config: `SIERPINSKI_TIER_HP/RADIUS/SPEED/SCORE/DEPTH` arrays. Tier 0 is miniboss weight in separation. No more Shard spawns from Sierpinski.)
- `LifecycleSystem` extraction: **Complete** (`web/src/systems/lifecycle-system.ts` centralizes enemy + bullet trail lifecycle; `Game` no longer manually tracks `trailId` lifetimes)
- `CombatSystem` extraction: **Complete** (`web/src/systems/combat-system.ts` centralizes kill processing, heat, hitstop, kill signatures, and staggered child spawns; `web/src/spawner/enemy-factory.ts` and `web/src/core/run-stats.ts` extracted as shared dependencies)
- `SpawnSystem` extraction: **Complete** (`web/src/systems/spawn-system.ts` centralizes WaveManager spawn execution, caps, edge-push, spawn SFX, and formation telegraphs. Fixed a latent bug: `Game` previously reassigned `this.enemies` each frame, orphaning the array reference held by `CombatSystem`/`SpawnSystem` — child spawns now land in the shared array. `game.ts`: 1,856 → 1,664 lines.)
- `GravitySystem` extraction: **Complete** (`web/src/systems/gravity-system.ts` centralizes BlackHole attraction/absorption/supernova, bullet bending, player pull, grid wells, circle flocks, and the separation gravity-well exemption. Game-owned supernova feedback routed via `onSupernovaWarning`/`onSupernovaDetonate` callbacks. `game.ts`: 1,664 → 1,489 lines.)
- Data-driven enemy classification: **Complete** (`Enemy` now carries `family` (`EnemyFamily`), `isBouncer`, and `separationWeight` records set per-subclass. `CombatSystem.getEnemyFamily()` instanceof ladder deleted — kill VFX/SFX read `enemy.family`; separation weights read `enemy.separationWeight` (BlackHole 0, miniboss 0.25, else 1); bouncer deflection reads `enemy.isBouncer`. Remaining `instanceof` checks are only for concrete-type member access — BlackHole gravity API, `Sierpinski.tier`, `MiniMandel.parent`, `BlackHole.absorbedCount`.)
- `config.ts` split: **Complete** (constants moved into 11 domain files under `web/src/config/`; `config.ts` is now a barrel re-exporting them. All 176 exports preserved; consumer imports from `'./config'` unchanged.)
- `BossSystem` extraction: **Complete** (`web/src/systems/boss-system.ts` collapses the two structurally-identical Sierpinski + Mandelbrot encounter state machines into one generic `BossEncounter` template instantiated from per-boss config; Mandelbrot supplies an active-phase hook for minions/stage transitions. ~190 lines removed from `game.ts`: 1,488 → 1,238 lines. **Refactor plan complete.**)
- Square removed: **Complete** (Square enemy deleted entirely — file, spawn pools, kill VFX, SFX, config all removed)
- Haptics cleanup: **Complete** (All haptics calls removed except `haptics.supernova()` on BlackHole overload detonation)
- Miniboss gravity immunity: **Complete** (Mandelbrot + Sierpinski tier 0 have `gravityImmune = true`)
- Bullet gravity bending: **Complete** (Bullets curve near BlackHoles. Config: `BULLET_GRAVITY_STRENGTH = 0.15`. Modifies velocity + updates angle for diamond rotation.)
- Spawn overlap fix: **Complete** (Separation steering now applies in final 70% of spawn animation instead of waiting for it to finish)
- Ominous supernova: **Complete** (1.5s destabilize telegraph with visual effects + rising drone audio, then massive 400-particle detonation with screen flash, 300ms hitstop, enhanced BH death audio with metallic ring layer)
- Pause feature: **Complete** (Press P during gameplay to pause and show config panel; paused state freezes updates but preserves visuals)
- AI agent (self-playing bot): **Complete** (neuroevolution-trained MLP policy plays live via B key / `?bot=1`. New `web/src/ai/` + `web/src/sim/` + `web/scripts/train.ts`,`eval.ts`. Enabled by: renderer/GL classes imported as `import type` so a headless import graph loads no WebGL/GLSL; `Player` depends on `InputSource` (DOM `Input` + headless `ScriptedInput`); `separateEnemies` extracted to `systems/separation.ts` shared by `Game` + twin. `window.game` exposed for test assertions. See `docs/AI_AGENT.md`. Flow test: `tests/flows/73-ai-agent-plays.yml`.)
- Live training dashboard: **Complete** (`scripts/train.ts --live [--port=8787]` starts a zero-dependency Node `http` server rendering `scripts/train-dashboard.html`: real-time fitness chart, per-phase survival/score of current best, gen/candidate progress, evolving per-layer weight heatmaps. Reward now CLI-tunable via `--survW`/`--scoreW`; `--phases` selects training phases. CEM loop yields via `setImmediate` per candidate to keep the server responsive.)
- AI Wingman (co-op ally): **Complete** (`web/src/entities/wingman.ts` — a standalone cyan ship driven by its own `Bot` (same trained policy), observing/acting from its own position. Enabled via the **AI Wingman** settings toggle (`gameSettings.aiWingman`, default off; toggle any time incl. pause menu — `Game.syncWingman()` creates/destroys it each frame). Bullets go into the shared `BulletPool` so kills score for the team; it reuses the player's weapon stage and is a non-colliding helper (can't be killed). Shows a "🤖 AI WINGMAN" badge. Settings panel now supports multiple checkboxes (data-driven `CHECKBOXES` keyed by `data-key`). Flow test: `tests/flows/74-ai-wingman.yml`.)
- Weapon shoot feedback: **Complete** (Player firing has its own procedural shotgun blast — `AudioManager.playShoot(pellets)`, 3 layers, subtly beefier/lower as parallel pellets rise 2→6 — plus a **subtle, player-local** visual recoil: the ship nudges back along the aim vector and springs over 90ms, and a small muzzle flash blooms from the barrel. Deliberately **no camera shake** (the initial version punched the camera and felt jarring at the ~3/s cadence). Driven from `Game.update()` when `player.tryShoot()` returns shots. Config in `config/player.ts`: `PLAYER_RECOIL_BASE/PER_PELLET/DECAY`, `PLAYER_MUZZLE_FLASH_LENGTH/PER_PELLET`. Wingman fires silently (no sound/recoil) to avoid doubling. Flow test extends `tests/flows/75-weapon-shotgun.yml`.)
- Specimen Gallery: **Complete** (`web/src/gallery.ts` — standalone visual catalog booted via `?gallery=1`, bypassing `Game` from `index.ts`. Instantiates every wired ship/enemy/child/boss/BlackHole-variant plus projectiles, explosions, crosshair, trail, and looping spawn animations, laid out in a labeled auto-fit grid for screenshot-based visual review. Reuses the real render pipeline: `Renderer` + `BloomPass` + `SpringMassGrid` + `TrailSystem` + `Starfield`. Enemies animate in place (updated then re-pinned to their cell). HTML label overlay (`#gallery-labels`). `window.gallery` exposed with `paused`/`gridOn`/`bloomOn`/`starfieldOn`/`labelsOn` toggles (Space/G/B/S/L). Flow test: `tests/flows/76-gallery.yml`.)
- Threat Lab: **Complete** (`web/src/threat-lab.ts` — playable `?threat=1` arena for A/B testing BlackHole threat presets: gravity strength/capture radius, HP, warning window, detonation payload, and 4 supernova sound variants. Built to answer "the BlackHole feels non-threatening". Engine additions: `BlackHole.destabilizeDuration`, `SUPERNOVA_DESTABILIZE_MS` config, `AudioManager.playSupernovaWarning(durationMs)` + `playSupernovaVariant()` with 'subdrop'/'doom'/'quake' deep-bass detonations. Flow test: `tests/flows/77-threat-lab.yml`.)
- BlackHole threat port: **Complete** (the user's Threat Lab picks shipped to the real game: **SINGULARITY-class gravity** — `bhAttractRadius` 500, `bhEnemyPull` 24, `bhPlayerPull` 9 defaults (settings loader migrates old ≤5-scale saves) + inescapable core (`BH_CORE_RADIUS_FRACTION` 0.8, `BH_CORE_PULL_MULT` 2.5 → tracking rhombuses captured within ~400px); **3× bullet bending** (`BULLET_GRAVITY_STRENGTH` 0.45); **CATACLYSM payload** — `SUPERNOVA_DESTABILIZE_MS` 350, `CIRCLE_SUPERNOVA_SPAWN_MULTIPLIER` 3, `SUPERNOVA_SHARD_COUNT` 8, 1000 particles, 3 shockwave rings (`GravitySystem.update()`/`renderEffects()`), 450ms flash; `BLACKHOLE_HP` 12; **subdrop detonation** (`SUPERNOVA_SOUND_VARIANT` in `config/audio.ts`); **stress-wobble audio** — `AudioManager.setBlackHoleStress(level)` continuous detuned-sub + tremolo loop that builds with absorbed mass, so the sequence is wobble → short warning → burst. Also fixed a latent bug: detonation was only checked inside the absorb branch (unreachable at full mass), so an unshot overloaded BH never exploded — now checked every frame. Flow test: `tests/flows/78-supernova-chaos.yml`.)
- Particle Lab + `ParticleField`: **Complete** (`web/src/renderer/particle-field.ts` — reusable ambient "cosmic dust" system: additive motes in world space with radial pull + **tangential swirl** toward attractors → glowing accretion disks, velocity-stretched streaks with speed→brightness/hue, camera-view wrapping, and `spawnBurst()` transient bursts. `web/src/particle-lab.ts` — playable `?particles=1` sandbox booted from `index.ts`, wiring four live-toggle/tunable effects: **A** dust field, **B** swirl-on-gravity (tangential term on enemy attraction so rhombuses orbit in), **C** thruster wake, **D** impact sparklets. Config: `PARTICLE_FIELD_*` in `config/effects.ts`. `window.particleLab` exposed. Not yet in the live game — preview/tuning stage. Flow test: `tests/flows/80-particle-lab.yml`.)
- Particle effects ported to the live game: **Complete** (the Particle Lab picks are now wired into real gameplay via `game.ts` `updateParticles()` + `renderEnemiesWarped()`. **Ambient dust** (`ParticleField`, `game.field`; density `PARTICLE_FIELD_GAME_DENSITY` 420 / mobile 120) with **life-stage heat coupling** — attractors built from active BlackHoles scale pull with swallowed mass and set `heat` (hue bias to amber-hot) that spikes while destabilizing, `field.brightness` rises too; rendered in the additive pass. **Geometry shatter** (`DebrisField`, `game.debris`) — `CombatSystem.emitShatter()` breaks killed polygonal units (rhombus/pinwheel/small-Sierpinski/default, guarded by `getWorldPoints().length>=3`) along their own edges into tumbling shards + a colour-tinted spark puff (`field.spawnBurst`); the generic particle "cloud" is cut to a small flash for those (bosses/BlackHole/circles keep their radial bursts). **Tidal death warp** — `renderEnemiesWarped()` wraps each non-BH enemy within `max(DEATH_WARP_REACH_MIN 150, hole.collisionRadius*DEATH_WARP_REACH_MULT 3.5)` of a hole in `Renderer.setWarp`, so units spaghettify toward the core. **Thruster wake** — dust kicked out behind the moving ship. **Subtle gravity swirl** — `GRAVITY_ENEMY_SWIRL` 0.35 tangential term in `GravitySystem.applyAttraction` so enemies spiral into holes. Headless training twin uses no-op `stubField`/`stubDebris` (CombatSystem only calls `spawnBurst`/`shatter`). New config in `config/effects.ts`. Flow test: `tests/flows/82-game-particles.yml`. Mobile-tuned density; 120fps in-game with 3 holes.)
- BlackHole spawn grace: **Complete** (BlackHoles now warp in over `SPAWN_DURATION_BLACKHOLE` = 3.0s (`config/spawner.ts`) instead of the 1.5s default — collision + gravity already skip `isSpawning` holes, so this is a long *harmless* telegraph that stops a lethal gravity well from materializing on top of the player; `SpawnSystem` ambush override uses `Math.max` so it never shortens it. Flow test: `tests/flows/81-blackhole-spawn-grace.yml`.)
- Per-BlackHole "personality": **Complete** (each spawned hole is its own beast — the `BlackHole` constructor randomises a unique blend of the Particle-Lab knobs: `visualMode` (random of dense/haze/corona/molten — the game previously only ever used `dense`), `dustStrengthMult` (0.55–1.65, accretion-disk brightness/tightness), `dustRadiusMult` (1.4–2.3, disk reach), `dustSwirl` (0.15–1.4, orbit vs. straight infall — per-attractor via the new `FieldAttractor.swirl` override), `enemySwirl` (0.1–0.7, replaces the global `GRAVITY_ENEMY_SWIRL` in `GravitySystem` — how much enemies spiral in), and `warpStretchMult`/`warpTwistMult` (tidal death-warp variation, applied in `renderEnemiesWarped`). Labs/gallery still override `visualMode` explicitly after construction. So no two holes look or behave the same.)
- BlackHole hit feedback + supernova shard removal + shatter visibility: **Complete** (three follow-up polish fixes. **Hit feedback**: shooting a BlackHole no longer flashes a flat white disc overlay — `registerHit()` kicks a `hitPulse` (outward ring pulse on the accretion ring) + emits a puff of `hitSparks` velocity-stretched streaks that fan out from the impact and fade (`renderHitFeedback()` replaces `renderHitFlash()`; `onBulletHit` passes the real bullet angle). Sparks are drawn as `s.vx * 55` streaks — `vx` is px/**ms** (~0.1–0.5), so an early `* 5` scale made them sub-pixel/invisible; 14 fast sparks per hit read as a clear puff. **Supernova shards removed**: `SUPERNOVA_SHARD_COUNT` 8→0 — the little triangle ejecta read as noise at gameplay zoom; circles still carry the supernova ejecta (Threat Lab keeps its own independent `shardCount`). **Shatter now legible in-game**: `SHATTER_EDGE_SUBDIV` 2→1 (each shard is a literal shape edge), `SHATTER_EJECT_SPEED` 0.10→0.13, `SHATTER_LIFE` 0.45→0.5, linear (not quadratic) alpha fade, and `DebrisField.render` now draws each shard as a bright core line + two dimmer parallel offset lines (`SHATTER_THICKNESS`) so it reads as a solid strut rather than a hairline. Also restored the `dense` variant's orbit-dot particles (a prior over-eager removal — they weren't the "tiny triangles" the user meant).)
- Geometry shatter + tidal death warp + life-stage dust (Particle Lab): **Complete** (three interlocking "physical geometry" effects, lab-only for now. **Geometry shatter** (`web/src/renderer/debris-field.ts` — `DebrisField`): a bullet/contact kill breaks the unit along its own wireframe edges into rigid, tumbling per-edge line fragments carrying the impact momentum — replaces the generic radial particle cloud for regular units; cheap enough to build live (no precompute). **Tidal death warp**: `Renderer.setWarp/clearWarp` add a per-vertex spaghettification transform — geometry within `warpReach` px of a hole is pulled radially inward (stronger closer in → it stretches) and twisted (frame drag); driven per-enemy in the lab render loop, the hole itself renders clean. **Life-stage dust coupling**: `FieldAttractor.heat` (0..1) biases nearby `ParticleField` motes toward amber-hot hues + `field.brightness` rises while destabilizing, so the dust heats/tightens as the hole fills and erupts on supernova; lab **F**/**T** keys drive the full feed→destabilize→detonate→recycle cycle. Config in `config/effects.ts` (`SHATTER_*`, `DEATH_WARP_*`). Flow test extends `tests/flows/80-particle-lab.yml`. **Not yet ported to the live game.**)
- Directional death shatter: **Complete** (kill fragments conserve bullet momentum — `collision.ts` records `impactAngle` per kill (bullet travel direction; contact kills use enemy velocity), `Explosion.init/spawn` take an optional `direction` that fans fragments forward in a ±77° triangular cone (`DEATH_FRAGMENT_CONE/SIDE_DAMPING/FORWARD_BOOST` in `config/effects.ts`) — nothing flies back toward the shooter. Applied to regular-unit kills + kill-signature rays; boss/BlackHole/supernova deaths stay radial. Flow test: `tests/flows/79-directional-shatter.yml`.)
- Taxonomy Lab (BlackHole effect anatomy): **Complete** (`web/src/taxonomy-lab.ts`, `?taxonomy=1` — a labeled anatomy chart naming every visual effect on a BlackHole, built to answer "there's something swirling around the hole and I don't know what it's called". Reveals the three in-class particle systems distinct from the external `ParticleField` dust: **Swirl Arms** (`swirlParticles`, 4 spiral accretion streams = the "swirling thing"), **Orbit Dots** (`horizonParticles`), **Infall Streaks** (`infallStreaks`) — each rendered solo via new `BlackHole.showSwirlArms/showOrbitDots/showInfallStreaks` flags (default true → shipped hole unchanged; guarded in `renderSwirlArms`/`renderInfallStreaks` + the 4 inline orbit-dot loops). Grid also shows the 4 accretion-disc modes + dust field + destabilize telegraph + hit sparks, each labeled with its source field. `window.taxonomyLab` exposed. Flow test: `tests/flows/85-taxonomy-lab.yml`.)
- Circle Lab (tracking + DNA sandbox): **Complete** (`web/src/circle-lab.ts`, `?circles=1` — a playable sandbox to pick the Circle enemy's tracking *feel* and BlackHole *visual DNA*, built because the shipped hard-lead tracking "feels gimmicky, needs elasticity/unpredictability". Five A/B tracking models switchable live — direct lead / **orbital (player-as-gravity-well: spring pull + momentum + drag → overshoot-orbit-fallback, the headline elastic feel)** / elastic spring / serpentine wander / swarm boids — plus toggleable visual-DNA layers: orbiting satellites, shed dust into a shared `ParticleField`, pulse halo, velocity streak, dark-core + accretion-ring body. Momentum-carrying `LabCircle`s with per-instance personality. Booted from `index.ts` alongside the other labs; `window.circleLab` exposed. **Not yet ported to the live game** — preview/tuning stage. Flow test: `tests/flows/84-circle-lab.yml`.)
- Circle orbital tracking + BlackHole DNA (Circle Lab picks ported): **Complete** (the shipped hard-lead circle tracking "felt gimmicky"; replaced with the Circle Lab's **orbital** model — the player is treated as a BlackHole. `CircleEnemy.update` now carries **persistent velocity** (momentum) and applies a central spring pull toward the player (`CIRCLE_ORBIT_SPRING` 1.4e-6 × distance, so a straying circle is always reeled back — bound orbits) + **low drag** (`CIRCLE_ORBIT_DRAG` 0.995, so orbits sustain then slowly spiral in) + speed cap (`CIRCLE_ORBIT_SPEED_CAP` 0.55). The supernova eject burst is **consumed once** into velocity as initial momentum, and `GravitySystem.detonate` now adds a **tangential kick around the player** (`CIRCLE_ORBIT_KICK_MIN/MAX` 0.28–0.44, random handedness; `CIRCLE_EJECT_RADIAL_SHARE` 0.5 keeps half the radial burst) so circles have angular momentum and *orbit* instead of falling straight in and instantly dying — verified: 36 circles orbit at ~0.35 px/ms for ~1.5s before spiralling in. **Ambient dust DNA**: each circle sheds a dust mote behind its motion into `game.field` (`updateParticles`, blue hue 205), so a flock reads as a drifting dusty cloud. **Hit sparks**: `CombatSystem` `case 'circle'` throws a gold velocity-stretched spark puff + blue dust burst in the bullet direction on kill (like shooting the hole itself). **No trail**: new `Enemy.hasTrail` flag (default true) is `false` on `CircleEnemy`; `LifecycleSystem.spawnEnemy` skips trail registration when false. The old flock-cohesion machinery (`flockCenter`, `GravitySystem.updateFlocks`, `circleFlocks`) is kept intact but vestigial for the game (still used by Threat Lab). Config in `config/enemy.ts` (`CIRCLE_ORBIT_*`, `CIRCLE_EJECT_RADIAL_SHARE`; removed `CIRCLE_LEAD_MAX_MS`/`CIRCLE_COMMIT_RADIUS`/`CIRCLE_FLOCK_PULL`/`CIRCLE_EJECT_DECAY`). Flow test: `tests/flows/83-circle-predictive-tracking.yml` (orbital momentum + no-trail + spring-bound).)
- Phase 4 (Scores, Polish & Tuning): **Not started** — leaderboard, debug overlay, perf profiling

---

## Known Technical Debt

- WAV audio files are large (could convert to OGG/MP3)
- Procedural music uses setTimeout (not AudioContext scheduler — may drift)
- No spatial partitioning for collision (O(bullets*enemies) fine at current scale)
- 9 unwired enemy source files (re-enable via `EnemyType`, `createEnemy()`, spawn pools)
- No leaderboard or debug overlay yet

---

## Design Documents

- **`PRD.md`** — Full product requirements
- **`ENEMY_DESIGNS.md`** — All enemy type designs (shapes, animations, mechanics)
- **`TASKS.md`** — Phase-by-phase task checklist
- **`docs/gridwars54-analysis/`** — Grid Wars 5.4 research pack (alignment guide, asset manifests, music manifests)

---

## Important Conventions

- **All constants in `config.ts`.** New tunables go in the matching domain file under `web/src/config/` (re-exported by the `config.ts` barrel).
- **Enemy classes are self-contained** (shape, colors, AI, rendering). Cross-system mechanics are driven by behavior records on `Enemy` — `family` (kill VFX/SFX), `isBouncer` (separation ricochet), `separationWeight` (separation push), `isMiniboss`, `gravityImmune` — set as `override` fields per subclass, so systems read fields instead of `instanceof`.
- **Object pooling** for bullets and explosions. Enemies are not pooled.
- **Additive blending** for trails/explosions. Normal blending for entities.
- **Mobile detection** via `'ontouchstart' in window` (reduced bloom, particles, trails).
- **No external runtime dependencies.** Vanilla TypeScript + WebGL + Web Audio.
- **Commit working state.** Verify `npm run build` succeeds before committing.
- **Playwright tests**: Headed by default (WebGL renders). Pass `--headless` to opt out.
